type User {
  id: ID!
  name: String!
}

type Query {
  users: [User!]!
  getUser(id: Int!): User
}

# Опционально: мутации для создания/обновления
type Mutation {
  createUser(name: String!): User!
}
